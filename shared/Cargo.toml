[package]
name = "shared-types"
version.workspace = true
edition = "2024"

[features]
default = []
image-compressor = []
json-formatter = []

[dependencies]
serde = { version = "1", features = ["derive"] }